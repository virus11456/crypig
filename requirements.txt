# 核心
pydantic>=2.6
PyYAML>=6.0
httpx>=0.27

# 市場資料 / 指標（接真實資料時）
ccxt>=4.2

# 排程
APScheduler>=3.10

# API
fastapi>=0.110
uvicorn[standard]>=0.29

# LLM（provider=claude 時）
anthropic>=0.40

# 註：核心知識圖譜為純 Python 實作，骨架不需額外相依即可跑。
# 若改用正式圖資料庫，再視 backend 安裝 neo4j 或 rdflib。
