[build]
builder = "nixpacks"

[deploy]
# 綁 Railway 提供的 $PORT，對外開放看板與 API
startCommand = "uvicorn crypig.dashboard.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/decisions"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
