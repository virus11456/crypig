# 第二十六批：LTH 日資料共用

實作版本：`6ecbac9b883264056de09e2aaf05426c08707798`。正式分支：`claude/brave-ptolemy-nn1nd8`。

## 變更

預設BTC LTH分析與歷史圖表改讀同一個持久化背景快照，取消分析每輪另抓 `/last`。成功資料最多重用6小時，UTC換日重查，失敗後退避1小時。沿用原 `lth_daily_supply_v1` 歷史快取；重啟不創造新歷史或重設取得時間。

共用資料為單進程服務，回傳獨立副本。來源日期倒退、日值衝突、不一致的已存快取均不取代有效資料。保留缺日與有效零；冷啟動回503、分析記no_data，背景完成後才使用真實資料。來源供給仍是資訊描述，不投方向票。自訂LTH指標或獨立建立agent保留原行為。

畫面分開顯示來源日期距UTC今天幾天、取得時間、刷新與保存失敗。舊後端缺少新增欄位時不假造來源年齡，仍能顯示原有取得時間。7／30天切換不增加上游請求。

## 驗證

84項Python、39項前端測試全部通過。HTTP MockTransport確認分析與接口並發僅一次取得、6小時到期、UTC跨日、429退避、恢復、重啟還原、來源日期倒退、缺日、零值、不可變副本與mock隔離。Vercel靜態建置成功。

Vercel的6ecbac9已Production Ready，部署識別 `HBvYh3ue1YksMkr52aQRAQs4rsCH`。實際HTML與提交來源一致；Chrome確認取得時間與7／30天切換正常。

## 正式部署完成（2026-09-14）

VPS建置提交 `cae2800352c6bc3bda5387ad5af0dc3ae6e37688`（包含功能6ecbac9）。使用者完成主控台登入後，核對乾淨目錄與既有版本，建立本次獨立程式／資料備份及回復映像；SQLite線上備份通過quick_check。舊同名備份目錄已存在，保留原件，改用本次專屬名稱，未覆蓋。

停用scheduler的臨時容器8個接口全部200，LTH新增metadata斷言通過，才更新crypig服務。容器於03:02:25 UTC啟動，後續healthy。映像 `sha256:d45a23fd5fe06f2627e9a9df39a802b1a4902ea13a00250b6bd384ba21c8897f`；44個Python檔案合併SHA256 `f2d53590540a45d93b3a2336c0832de06ad474e1d671259562a9d079a20732e2`，與本機一致。本批未建立SSH金鑰，也未更新其他服務。

兩站各17個主要讀取路徑以及www首頁全部200，兩站首頁完全一致，穩定資料一致；即時行情年齡隨請求時間變化。Chrome實測hypeboss.cc的7／30天切換與新來源日期／取得時間說明正常。

LTH來源日期2026-09-13、來源年齡1天，取得時間1789352476.9915009沿用已存快照，未因部署改寫。新metadata顯示6小時刷新／1小時失敗退避。新分析於03:03:11.702993 UTC完成，restored=false、stale=false、last_cycle_failed=false；LTH步驟0.002秒（單次觀察，非節省比例）。驗證期間行情背景刷新曾失敗但仍保留舊值及原時間，不能把整輪完成當成所有來源刷新成功。

本次備份與回復名稱記於本機outputs的上線紀錄。僅回復程式時可將本次rollback映像重新標記為crypig:latest，再以docker compose up -d --no-deps --no-build crypig更新該服務。資料沒有格式遷移；資料還原會覆蓋部署後累積紀錄，非必要不執行。

仍未量測正式上游請求節省比例。六小時TTL是本產品抓取策略，不是來源發布保證。仍需處理Hyperliquid市場快照與資格榜單去重；真實LLM未串接。
